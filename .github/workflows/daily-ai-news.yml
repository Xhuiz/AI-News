name: Daily AI News

on:
  push:
    branches:
      - main
    paths:
      - .github/workflows/daily-ai-news.yml
      - .github/automation-trigger.txt
  schedule:
    - cron: "17 11 * * *"
    - cron: "47 11 * * *"
    - cron: "17 12 * * *"
    - cron: "45 12 * * *"
    - cron: "15 13 * * *"
    - cron: "45 13 * * *"
  workflow_dispatch:
    inputs:
      report_date:
        description: Beijing report date formatted as YYYY-MM-DD.
        required: false
        type: string
      dry_run:
        description: Generate the report without sending email, marking sent, or committing reports.
        required: false
        default: false
        type: boolean

permissions:
  contents: write

concurrency:
  group: daily-ai-news-${{ github.ref }}
  cancel-in-progress: false

jobs:
  daily-ai-news:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v6
        with:
          fetch-depth: 0

      - name: Set up Python
        uses: actions/setup-python@v6
        with:
          python-version: "3.11"

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          python -m pip install -e ".[dev]"

      - name: Run tests
        run: pytest -v

      - name: Resolve trigger context
        id: trigger_context
        run: |
          if [ "${{ github.event_name }}" = "push" ] && [ -f ".github/automation-trigger.txt" ]; then
            report_date="$(sed 's/^\xEF\xBB\xBF//' .github/automation-trigger.txt | grep -E "^REPORT_DATE=" | tail -n 1 | cut -d= -f2-)"
          elif [ "${{ github.event_name }}" = "workflow_dispatch" ] && [ -n "${{ inputs.report_date }}" ]; then
            report_date="${{ inputs.report_date }}"
          elif [ "${{ github.event_name }}" = "schedule" ]; then
            report_date="$(date -u +%F)"
          else
            report_date="$(TZ=Asia/Shanghai date +%F)"
          fi
          if ! echo "$report_date" | grep -Eq "^[0-9]{4}-[0-9]{2}-[0-9]{2}$"; then
            echo "Invalid report date: $report_date" >&2
            exit 1
          fi
          echo "report_date=${report_date}" >> "$GITHUB_OUTPUT"
          echo "Report date: ${report_date}"

      - name: Check today's email marker
        id: email_marker
        run: |
          report_date="${{ steps.trigger_context.outputs.report_date }}"
          marker_path="reports/.email-sent-${report_date}"
          echo "date=${report_date}" >> "$GITHUB_OUTPUT"
          echo "path=${marker_path}" >> "$GITHUB_OUTPUT"
          if [ -f "$marker_path" ]; then
            echo "exists=true" >> "$GITHUB_OUTPUT"
            echo "Email marker already exists: $marker_path"
          else
            echo "exists=false" >> "$GITHUB_OUTPUT"
            echo "Email marker not found: $marker_path"
          fi

      - name: Generate and email daily report
        if: github.event_name == 'workflow_dispatch' || steps.email_marker.outputs.exists != 'true'
        env:
          AI_API_KEY: ${{ secrets.GEMINI_API_KEY || secrets.AI_API_KEY }}
          AI_BASE_URL: https://generativelanguage.googleapis.com/v1beta/openai
          AI_MODEL: gemini-3.6-flash
          AI_API_STYLE: chat_completions
          DRY_RUN: ${{ inputs.dry_run || 'false' }}
          MAIL_HOST: ${{ secrets.MAIL_HOST || 'smtp.163.com' }}
          MAIL_PORT: ${{ secrets.MAIL_PORT || '465' }}
          MAIL_USERNAME: ${{ secrets.MAIL_USERNAME }}
          MAIL_PASSWORD: ${{ secrets.MAIL_PASSWORD }}
          MAIL_FROM: ${{ secrets.MAIL_FROM }}
          MAIL_TO: ${{ secrets.MAIL_TO }}
          NEWS_API_PROVIDER: ${{ secrets.NEWS_API_PROVIDER }}
          NEWS_API_KEY: ${{ secrets.NEWS_API_KEY }}
        run: python -m ai_news.main --report-date "${{ steps.trigger_context.outputs.report_date }}"

      - name: Mark email sent
        if: (github.event_name == 'workflow_dispatch' && inputs.dry_run != true) || (github.event_name != 'workflow_dispatch' && steps.email_marker.outputs.exists != 'true')
        run: |
          touch "${{ steps.email_marker.outputs.path }}"

      - name: Commit report
        if: github.event_name != 'workflow_dispatch' || inputs.dry_run != true
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          if compgen -G "reports/*.md" > /dev/null || compgen -G "reports/.email-sent-*" > /dev/null; then
            git add reports
          else
            echo "No report or email marker files found."
          fi
          if git diff --cached --quiet; then
            echo "No report changes to commit."
          else
            git commit -m "chore: add AI news report for $(date -u +%F)"
            git fetch origin main:refs/remotes/origin/main
            git rebase origin/main
            git push origin HEAD:main
          fi
